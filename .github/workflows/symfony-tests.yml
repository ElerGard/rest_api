name: Symfony

on:
  push:
    branches: [ main ]

jobs:
  symfony-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Execute tests (Unit and Feature tests) via PHPUni
        with:
          path: vendor
        run: vendor/bin/phpunit